import { DashboardExample } from '@/examples/DashboardExample';

/** Root of the example app shipped with the template. Replace with your own screens. */
export function App() {
  return <DashboardExample />;
}
